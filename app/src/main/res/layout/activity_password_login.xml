<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_password_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="cn.edu.ecust.faceaccesscontrol.activity.PasswordLoginActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/passwordloginactivity_toolbar_common"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/keepBrown"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <EditText
            android:id="@+id/passwordloginactivity_edittext_password"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:layout_centerInParent="true"
            android:hint="请输入密码"
            android:background="@color/buttonWhite"
            />

        <EditText
            android:id="@+id/passwordloginactivity_edittext_no"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:layout_above="@+id/passwordloginactivity_edittext_password"
            android:layout_centerHorizontal="true"
            android:layout_margin="16dp"
            android:hint="请输入工号"
            android:inputType="textPassword"
            android:background="@color/buttonWhite"/>

        <Button
            android:id="@+id/passwordloginactivity_button_login"
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:layout_below="@+id/passwordloginactivity_edittext_password"
            android:layout_centerHorizontal="true"
            android:layout_margin="16dp"
            android:text="登录"
            android:background="@color/buttonWhite"/>
    </RelativeLayout>
</LinearLayout>
